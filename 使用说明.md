# 美太 AI 商业创新智能体 — 使用说明

## 项目简介

美太 AI 商业创新智能体是一个面向企业 AI 转型咨询的 Demo 系统。通过结构化的问卷→画像→诊断→推荐→报告流程，帮助企业完成从现状评估到 AI 创新方案生成的完整链路。

**核心能力**：14 步标准咨询流程、Mock/Live 双模式运行、Markdown/DOCX/PDF 多格式报告导出、讲师工作台。

---

## 系统界面概览

系统为单页应用（SPA），无全局导航栏。所有页面通过按钮和面包屑链接串联。

### 首页

打开浏览器访问 `http://localhost:3001`，看到首页：

- **顶部区域**：项目标题「美太 AI 商业创新智能体 Demo」、当前阶段标签、两个入口按钮——「导入课前材料」和「开始企业问卷」。
- **左侧「健康检查」卡片**：显示后端 /health 接口的连通状态。
- **右侧「当前范围」卡片**：列举 7 项已打通的链路里程碑。
- **底部「LLM 运行时配置」卡片**：可在页面上动态切换 LLM 模式、API Key、Base URL 和模型名，无需重启服务。

### 问卷工作台（/assessment）

点击「开始企业问卷」进入。页面分两个标签页：

- **学员视角**（默认）：
  - 左侧是 11 字段的企业问卷表单（公司名称、行业、规模、地区、年收入、核心产品、目标客户、当前痛点、AI 目标、可用数据、备注）。
  - 右侧是工作流侧边栏，显示 6 个步骤节点和操作按钮。每完成一步，对应节点亮起，下一步解锁。
  - 底部展示 6 张结果卡片，显示各模块的最新生成状态。
- **讲师视角**：学员总览表格，支持按班级分组筛选、批量点评、CSV 导出。

问卷提交后自动跳转到 `/assessment/{id}`，后续所有操作都在该页面完成。

### 课前导入工作台（/intake）

点击首页「导入课前材料」进入。两栏布局：

- **左栏（步骤 1）**：支持三种导入方式——粘贴文本、上传文件（TXT/Markdown/PDF/DOCX）、手动填表。也可通过 Session ID 回溯历史导入。
- **右栏（步骤 2-3）**：AI 自动提取字段并预填显示，支持人工修正后确认创建问卷。

---

## 快速启动

### 环境要求

| 组件 | 版本 |
|------|:---:|
| Python | ≥ 3.11（conda 环境 `meitai-project`） |
| Node.js | ≥ 18 |

### 安装依赖

```powershell
# 后端
cd backend
pip install -r requirements.txt

# 前端
cd frontend
npm install
```

### 配置密钥（可选，Mock 模式可跳过）

默认 Mock 模式即开即用，无需 API Key。如需真实 LLM：

1. 复制 `mykey.py.example` → `backend/app/core/mykey.py`
2. 填入 API Key 和 Base URL
3. 将 `.env` 中 `LLM_MODE` 改为 `live`

支持所有 OpenAI 兼容接口（DeepSeek、千问、智谱等），只需修改 Base URL 和模型名。

### 启动

**方式一：一键启动**

```powershell
.\start.bat
```

自动依次启动后端 → ngrok 隧道 → 前端。

**方式二：分别启动**

```powershell
# 终端 1 — 后端
conda activate meitai-project
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 终端 2 — 前端
cd frontend
npx next dev -p 3001
```

**方式三：使用启动脚本**

直接双击运行 `scripts/back_start.bat` 和 `scripts/front_start.bat`。

### 访问地址

| 页面 | URL |
|------|-----|
| 首页 | `http://localhost:3001` |
| 新建问卷 | `http://localhost:3001/assessment` |
| 指定问卷 | `http://localhost:3001/assessment/{id}` |
| 企业画像详情 | `http://localhost:3001/assessment/{id}/profile` |
| 画布诊断详情 | `http://localhost:3001/assessment/{id}/canvas` |
| 综合结果面板 | `http://localhost:3001/assessment/{id}/results` |
| 报告生成 | `http://localhost:3001/report/{id}` |
| 报告上下文 | `http://localhost:3001/report-context/{id}` |
| 报告文档 | `http://localhost:3001/reports/{reportId}` |
| 课前导入 | `http://localhost:3001/intake` |

---

## 完整操作流程（14 步）

建议按以下顺序逐步操作，每步结果自动保存，刷新页面后自动恢复。

### 第 1 步：创建企业问卷

在 `/assessment` 页面填写 11 个字段，点击「提交问卷」。系统生成唯一评估 ID，后续所有操作围绕该 ID 进行。

### 第 2 步：课前导入（可选）

在 `/intake` 页面上传课前材料（企业介绍、商业计划书等），AI 自动提取关键字段并预填到问卷。适用于提前收到企业资料的场景。

### 第 3 步：生成企业画像

提交问卷后，右侧工作流侧边栏出现「生成企业画像」按钮。点击后系统根据问卷数据生成 8 个模块的结构化企业画像（基本信息、业务特征、关键挑战、AI 方向、信息缺失等）。

### 第 4 步：商业画布 9 格诊断

生成画像后，点击「生成商业画布诊断」。系统从 9 个维度诊断企业商业模式（客户细分、价值主张、渠道、客户关系、收入来源、核心资源、关键活动、关键伙伴、成本结构），每个维度输出现状描述、诊断意见、AI 机会和缺失信息。

### 第 5 步：突破要素推荐与选择

画布诊断完成后，系统从 9 个商业要素中评分推荐 2-3 个突破方向。用户可在交互面板中选择/调整突破要素。

### 第 6 步：创新方向延展

选定突破要素后，系统为每个要素延展 6 个创新方向。用户勾选 1-6 个感兴趣的方向。

### 第 7 步：差异化竞争力分析

系统基于企业画像和选定的突破方向，重构价值主张（VP），串联产品-服务-生态的差异化连接，输出三阶段交付策略。

### 第 8 步：商业终局设计

从私域、生态、OPC（运营-产品-渠道）三个维度分析企业的商业终局场景，并给出 3 条战略路径建议。

### 第 9 步：AI 场景推荐

系统基于选定的创新方向进行加权评分，推荐 Top 3 高优先级 AI 应用场景（含评分解读、数据需求、实施建议）。

### 第 10 步：分层案例匹配

系统按「行业 → 规模 → 痛点 → 方向」四个层级检索匹配行业参考案例，每个案例标注来源和相关性。

### 第 11 步：生成最终报告

进入 `/report/{id}` 页面。系统先展示 5 项就绪检查（画像、画布、场景、案例、竞争力）。就绪后可选报告模式：

- **模板模式**（默认）：固定 14 章节结构，确定性输出，速度快。
- **LLM 模式**：使用 AI 增强报告内容，失败自动回退模板。

点击「生成报告」后跳转到报告文档页面。

### 第 12 步：报告导出

在报告文档页面（`/reports/{reportId}`），支持：

| 导出格式 | 按钮 |
|----------|------|
| Markdown | 下载 .md 文件 |
| Word | 下载 .docx 文件 |
| 打印版 | 打开适合打印的 HTML 页面 |
| PDF | 下载 .pdf 文件 |

另有「分享链接」功能，可生成匿名访问 Token。

### 第 13 步：课后跟进（前端已隐藏入口）

系统为每个评估自动生成 6 项默认跟进任务，支持 4 态流转（待办 → 进行中 → 已完成 → 已取消）。双周案例推送和复盘再校准也可通过 API 触发。

### 第 14 步：讲师工作台

在 `/assessment` 页切换到「讲师视角」标签：

- 查看所有学员的完成状态（画像/画布/突破/场景/报告图标）
- 按班级分组筛选
- 批量点评（输入后一键应用到勾选的学员）
- CSV 导出（含原始问卷 + 所有评分结果）

---

## 报告结构（14 章节）

| # | 章节 |
|---|------|
| 1 | 企业基本画像 |
| 2 | 当前商业模式画布诊断 |
| 3 | 突破要素 |
| 4 | 创新方向延展 |
| 5 | AI 成熟度评估 |
| 6 | 高优先级 AI 提效场景 |
| 7 | 推荐场景详细规划 |
| 8 | 差异化竞争力设计 |
| 9 | 参考案例与启示 |
| 10 | 三阶段 AI 创新路线图 |
| 11 | 90 天行动计划 |
| 12 | 风险与阻力 |
| 13 | 讲师点评区 |
| 14 | 商业终局设计 |

---

## 运行模式说明

| 模式 | 配置 | 特点 |
|------|------|------|
| Mock（默认） | `LLM_MODE=mock` | 规则引擎 + 模板，无需 API Key，结果确定性 |
| Live | `LLM_MODE=live` + API Key | 画像、画布、报告可走真实 LLM |
| 报告:模板 | `?mode=template` | 固定模板生成报告 |
| 报告:LLM | `?mode=llm` | LLM 增强，失败自动回退模板 |

---

## 级联清空机制

上游模块重新生成时，下游结果自动失效：

```
画像 → 画布 → 突破要素 → 场景 → 案例 → 报告
      └──── 方向延展 ────┘
      └── 竞争力分析 ────┘
      └── 商业终局 ──────┘
```

刷新页面后从后端自动恢复所有历史结果。无需手动清理。

---

## 运行时 LLM 配置

在首页底部「LLM 运行时配置」卡片中，可直接切换 LLM 模式：

1. 修改 API Key、Base URL、模型名
2. 切换 Mock/Live 模式
3. 配置实时生效（写入 `.env` + 后端热重载），无需手动重启

---

## 端口说明

| 服务 | 端口 |
|------|:---:|
| 前端 | 3001 |
| 后端 | 8000 |

---

## 运行测试

```powershell
# 后端单元测试（需 meitai-project 环境）
cd backend
conda run -n meitai-project python -m pytest tests/ -v

# 前端单元测试
cd frontend
npx vitest run

# 全链路 E2E 测试（26 步骤验证）
cd backend
conda run -n meitai-project python -m pytest tests/test_e2e_full_chain.py -v -s
```

---

## 常见问题

**Q: Mock 模式和 Live 模式有什么区别？**

A: Mock 模式使用内置规则引擎和模板，所有输出都是确定性的，适合 Demo 演示和测试。Live 模式调用真实 LLM，输出更个性化，但需要配置 API Key 且有网络延迟。

**Q: 如何切换 API 提供商？**

A: 编辑 `backend/app/core/mykey.py`（或在首页 LLM 配置卡片中修改），将 `openai_base_url` 设为目标地址（如 `https://api.deepseek.com`），`openai_model` 设为对应模型名，再将 `.env` 中 `LLM_MODE` 改为 `live`。

**Q: 报告生成失败怎么办？**

A: 先检查 `/report/{id}` 页面的 5 项就绪检查是否全部通过。LLM 模式下失败会自动回退到模板模式。如果模板模式也失败，检查后端日志。

**Q: 数据存储在哪里？**

A: 所有数据存储在 `backend/data/meitai_demo.db`（SQLite）。删除该文件会清空所有数据。ChromaDB 向量数据存储在 `backend/data/chroma/`。

**Q: 如何从公网访问？**

A: 使用 `start.bat` 一键启动会自动开启 ngrok 隧道，生成公网 URL。也可手动运行 `ngrok http 8000`。
