# 美太 AI 商业创新智能体 — 使用方法

## 环境要求

| 组件 | 版本 | 说明 |
|------|:---:|------|
| Python | ≥ 3.11 | conda 环境 `rag-env` |
| Node.js | ≥ 18 | 前端构建 |
| 数据库 | SQLite | 自动创建，无需安装 |

## 快速启动

### 1. 安装依赖

```powershell
# 后端
cd backend
pip install -r requirements.txt

# 前端
cd frontend
npm install
```

### 2. 启动

**后端**（端口 8000）：
```powershell
conda activate rag-env
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**前端**（端口 3001）：
```powershell
cd frontend
npx next dev -p 3001
```

或使用启动脚本：
- `scripts/back_start.bat`
- `scripts/front_start.bat`

### 3. 访问

| 页面 | 地址 |
|------|------|
| 首页 | `http://localhost:3001` |
| 新建问卷 | `http://localhost:3001/assessment` |
| 指定问卷 | `http://localhost:3001/assessment/{id}` |
| 报告预览 | `http://localhost:3001/report/{id}` |
| 课前导入 | `http://localhost:3001/intake` |

---

## API Key 配置

### 配置优先级

```
.env 环境变量  >  mykey.py  >  config.py 默认值
   (最高)          (中间)        (最低)
```

### Mock 模式（默认，无需 API Key）

`.env` 保持 `LLM_MODE="mock"`，所有生成走规则引擎/模板，确定性输出。即开即用。

### 切换到真实 LLM

**第一步**：编辑 `backend/app/core/mykey.py`，填入真实凭证：

```python
llm_config = {
    "llm_mode": "live",
    "openai_api_key": "sk-你的真实key",
    "openai_base_url": "https://api.openai.com/v1",
    "openai_model": "gpt-4o-mini",
    "llm_report_enabled": True,
    "llm_report_timeout_seconds": 60,
}
```

**第二步**：把 `.env` 中的 `LLM_MODE` 从 `"mock"` 改为 `"live"`（或直接删除该行让 mykey.py 生效）。

**非 OpenAI 兼容接口**（DeepSeek / 千问 / 智谱等）：只需改 `openai_base_url` 和 `openai_model` 为对应地址和模型名即可。

### 安全说明

- `mykey.py` 已在 `.gitignore` 中，不会被提交
- 模板文件 `mykey.py.example` 可安全提交
- `.env` 中不应存放真实 API Key

---

## 运行模式

| 模式 | 配置 | 说明 |
|------|------|------|
| Mock（默认） | `LLM_MODE=mock` | 规则引擎/模板，无需 API Key |
| Live | `LLM_MODE=live` + API Key | 画像/画布/报告可走真实 LLM |
| 报告：模板 | `mode=template` | 固定模板生成报告 |
| 报告：LLM | `mode=llm` | LLM 增强，失败自动回退模板 |

---

## 完整功能链路（14 步）

| # | 步骤 | API 端点 | 说明 |
|---|------|----------|------|
| 1 | 企业问卷 | `POST /api/assessments` | 11 字段录入 |
| 2 | 课前导入 | `POST /api/intake/import` | 支持文本/文件/表单 |
| 3 | 企业画像 | `POST /api/assessments/{id}/profile` | Mock 规则引擎 或 LLM |
| 4 | 商业画布诊断 | `POST /api/assessments/{id}/canvas` | 9 格诊断，九要素问题库增强 |
| 5 | 突破要素推荐 | `POST .../breakthrough/recommend` | 9 要素评分，选 2-3 个 |
| 6 | 创新方向延展 | `POST .../directions/expand` | 每要素 6 方向，勾选 1-6 个 |
| 7 | 差异化竞争力 | `POST .../competitiveness/generate` | VP 重构 + 点线串联 |
| 8 | 商业终局设计 | `POST .../endgame/generate` | 私域 + 生态 + OPC + 3 路径 |
| 9 | AI 场景推荐 | `POST .../scenarios` | 方向加权评分 |
| 10 | 分层案例匹配 | `POST .../cases` | 行业→规模→痛点→方向 |
| 11 | 报告生成 | `POST .../report?mode=template` | 14 章节 + 质量审计 |
| 12 | 多格式导出 | `GET /api/reports/{id}/export/*` | Markdown / DOCX / PDF / HTML |
| 13 | 课后 30 天跟进 | *（前端已隐藏）* | 6 项默认任务，4 态流转 |
| 14 | 讲师工作台 | `GET /api/instructor/dashboard` | 分组 + 批量点评 + CSV 导出 |

---

## API 端点快速参考

### 核心流程

| 方法 | 端点 | 说明 |
|------|------|------|
| `GET` | `/health` | 健康检查 |
| `POST` | `/api/assessments` | 创建问卷 |
| `GET` | `/api/assessments/{id}` | 完整聚合状态 |
| `POST` | `/api/assessments/{id}/profile` | 企业画像 |
| `POST` | `/api/assessments/{id}/canvas` | 画布诊断 |
| `POST` | `/api/assessments/{id}/breakthrough/recommend` | 突破要素推荐 |
| `POST` | `/api/assessments/{id}/breakthrough/select` | 突破要素选择 |
| `POST` | `/api/assessments/{id}/directions/expand` | 方向延展 |
| `POST` | `/api/assessments/{id}/directions/select` | 方向选择 |
| `POST` | `/api/assessments/{id}/competitiveness/generate` | 竞争力分析 |
| `POST` | `/api/assessments/{id}/endgame/generate` | 商业终局分析 |
| `POST` | `/api/assessments/{id}/scenarios` | 场景推荐 |
| `POST` | `/api/assessments/{id}/cases` | 案例匹配 |
| `POST` | `/api/assessments/{id}/report?mode=template\|llm` | 生成报告 |

### 报告与导出

| 方法 | 端点 | 说明 |
|------|------|------|
| `GET` | `/api/reports/{id}` | 报告详情 |
| `GET` | `/api/reports/{id}/export/markdown` | 下载 Markdown |
| `GET` | `/api/reports/{id}/export/docx` | 下载 DOCX |
| `GET` | `/api/reports/{id}/export/pdf` | 下载 PDF |
| `GET` | `/api/reports/{id}/print` | 打印版 HTML |
| `GET` | `/api/reports/{id}/quality` | 质量审计 |
| `POST` | `/api/reports/{id}/share` | 生成分享链接 |

### 讲师视角

| 方法 | 端点 | 说明 |
|------|------|------|
| `GET` | `/api/instructor/dashboard` | 学员总览 |
| `POST` | `/api/instructor/batch-comment` | 批量点评 |
| `GET` | `/api/instructor/export?format=csv` | CSV 导出 |

---

## 报告结构（14 章节）

1. 企业基本画像
2. 当前商业模式画布诊断
3. 突破要素
4. 创新方向延展
5. AI 成熟度评估
6. 高优先级 AI 提效场景
7. 推荐场景详细规划
8. 差异化竞争力设计
9. 参考案例与启示
10. 三阶段 AI 创新路线图
11. 90 天行动计划
12. 风险与阻力
13. 讲师点评区
14. 商业终局设计

---

## 运行测试

**后端**（需要 rag-env 环境）：
```powershell
conda activate rag-env
cd backend
python -m pytest tests/ -v
# 预期：19 passed, 1 skipped
```

**前端**：
```powershell
cd frontend
npx vitest run
# 预期：6 passed
```

**全链路 E2E**：
```powershell
cd backend
python -m pytest tests/test_e2e_full_chain.py -v -s
# 26 步骤验证
```

---

## 级联清空机制

上游模块重新生成时，下游结果自动失效：
```
画像 → 画布 → 突破要素 → 场景 → 案例 → 报告
      └──── 方向延展 ────┘
      └── 竞争力分析 ────┘
      └── 商业终局 ──────┘
```

刷新页面后自动从后端恢复所有历史结果。

---

## 环境变量完整参考

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `APP_ENV` | `development` | 运行环境 |
| `LLM_MODE` | `mock` | `mock` / `live` |
| `FRONTEND_ORIGIN` | `http://localhost:3001` | CORS 允许的前端地址 |
| `DATABASE_URL` | `sqlite:///./backend/data/meitai_demo.db` | SQLite 路径 |
| `RAG_ENABLED` | `false` | ChromaDB 向量检索 |
| `LLM_REPORT_ENABLED` | `false` | LLM 增强报告 |
| `NEXT_PUBLIC_API_BASE_URL` | `http://localhost:8000` | 前端调用后端地址 |
| `INTAKE_MAX_UPLOAD_SIZE_MB` | `10` | 导入文件大小上限 |
