"""
LLM API key configuration template for Meitai AI Business Innovation Agent.

→ 复制此文件为 mykey.py（已在 .gitignore 中）
→ 填入真实凭证
→ mykey.py 中的值会覆盖 .env 中的对应字段
"""

llm_config = {
    "llm_mode": "live",                     # "mock" | "live"
    "openai_api_key": "sk-你的真实key",
    "openai_base_url": "https://api.openai.com/v1",
    "openai_model": "gpt-4o-mini",
    "llm_report_enabled": True,             # 是否启用 LLM 增强报告
    "llm_report_timeout_seconds": 60,
}
